<div class="row">
  <div class="col-md-12">
    <div class="main-card mb-3 card">
      <div class="card-header">Appointments &nbsp;
        <!-- Button trigger modal -->
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#staticBackdrop">
          New Appointment
        </button>
        <div class="btn-actions-pane-right">
          <div role="group" class="btn-group-sm btn-group">
            <button class="active btn btn-focus">Last Week</button>
            <button class="btn btn-focus">All Month</button>
          </div>
        </div>
      </div>
      <div class="table-responsive">
        <table class="align-middle mb-0 table table-borderless table-striped table-hover">
          <thead>
          <tr>
            <th class="text-center">#</th>
            <th>Name</th>
            <th class="text-center">Unit</th>
            <th class="text-center">When</th>
            <th class="text-center">Status</th>
            <th class="text-center">Actions</th>
          </tr>
          </thead>
          <tbody>

          <% @appointments.each do |appointment| %>

          <tr>
            <td class="text-center text-muted">#<%=appointment.id %></td>
            <td>
              <div class="widget-content p-0">
                <div class="widget-content-wrapper">
                  <div class="widget-content-left flex2">
                    <div class="widget-heading"><%= appointment.user.name %></div>
                    <div class="widget-subheading opacity-7"><%= appointment.user.address %></div>
                  </div>
                </div>
              </div>
            </td>
            <td class="text-center">
              <div class="widget-content p-0">
                <div class="widget-content-wrapper">
                  <div class="widget-content-left flex2">
                    <div class="widget-heading"><%= appointment.unit.unit_no %></div>
                    <div class="widget-subheading opacity-7"><%= appointment.unit.unit_type.name %></div>
                  </div>
                </div>
              </div>

            </td>
            <td class="text-center"><%= appointment.datetime.strftime("%A, %-d %b at %I:%M %p") if appointment.datetime.present?  %></td>
            <td class="text-center">
              <div class="badge badge-warning"><%= appointment.status %></div>
            </td>
            <td class="text-center">
              <a href="<%= manager_lead_path(appointment) %>"><button type="button" id="PopoverCustomT-1" class="btn btn-primary btn-sm">Details</button></a>
            </td>
          </tr>
          <% end %>
          </tbody>
        </table>
      </div>

    </div>
  </div>

</div>
